apiVersion: v1
kind: Service
metadata:
  labels:
    app: minio
  name: minio-dev
  namespace: minio-dev
spec:
  ports:
  - port: 39000
    protocol: TCP
    targetPort: 9000
  selector:
    app: minio
  type: LoadBalancer
status:
  loadBalancer: {}
